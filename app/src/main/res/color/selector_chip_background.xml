<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active State -->
    <item android:color="@color/zen_accent" android:state_checked="true"/>
    <item android:color="@color/zen_accent" android:state_selected="true"/>
    <!-- Inactive State -->
    <item android:color="@android:color/transparent"/>
</selector>
